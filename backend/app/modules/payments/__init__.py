"""LEGACY_DORMANT — DO NOT EXTEND FOR PHASE 1.

Stripe seam, webhooks, disputes and reconciliation. Kept, tested and working, but outside the current product
phase: it returns only in Phase 2 (MONTHLY transactional), after a KEEP / ADAPT / REWRITE
audit. Phase-1 modules must not import it
(tests/test_phase1_boundaries.py). See
docs/canonical/IMPLEMENTATION-CONVERGENCE.md.
"""
