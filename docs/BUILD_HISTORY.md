# BUILD HISTORY

One entry per completed micro-cycle. Newest last. Status legend: ✅ shipped ·
⚠️ shipped with known gap · ⛔ blocked.

| # | Date | Scope | Tests | Security / verification | Commit | CI | Deploy |
|---|---|---|---|---|---|---|---|
| 01 | 2026-07-05 | Phase 0: monorepo skeleton, FastAPI walking skeleton, compose stack, CI | 1 | — | `7ec222f` | n/a | — |
| 02 | 2026-07-05 | Chat 01 closed: 6 ADRs, OpenAPI+AsyncAPI contracts, bounded-context map | — | Spectral 0 errors | `a675c7b` | n/a | — |
| 03 | 2026-07-05 | Business architecture package (17 bounded contexts, lifecycles, disputes) | — | — | `97c73f8` | n/a | — |
| 04 | 2026-07-05 | Hostile EA review: 15 perspectives, 100-item red team, gap analysis | — | — | `cd5f5bc` | n/a | — |
| 05 | 2026-07-05 | Strategy: operator-first model, Stripe Connect, KPI framework | — | — | `c4d1652` | n/a | — |
| 06 | 2026-07-05 | Founder Mode constitution + repo CLAUDE.md | — | — | `4742ff8` | n/a | — |
| 07 | 2026-07-05 | **D4 vertical slice + D5 hardening**: auth, listings, booking, payments (sim), ledger, admin | 12 | exclusion constraint, late-success refund, webhook secret, append-only guards | `4b5852f` | n/a | — |
| 08 | 2026-07-05 | **D6 warfare**: real adversarial runs on live Postgres | 12 | 60→1 booking, 200→1 capture, 10→1 payout, DB crash ACID | `dd3b88c` | n/a | — |
| 09 | 2026-07-05 | D7 production readiness board — evidence-based **NO-GO** (22/100) | — | evidence-only audit | `f032815` | n/a | — |
| 10 | 2026-07-05 | **D8**: release loop, Alembic migrations, DB append-only triggers | 12 | direct-SQL ledger tamper blocked (owner-proof) | `7a3fc50` | n/a | — |
| 11 | 2026-07-05 | **D9 disaster recovery**: encrypted backup, restore drill | 12 | restore RTO 4s, financial reconciliation PASS ×2 | `4989b50` | n/a | — |
| 12 | 2026-07-05 | **B1 Stripe Connect** adapter, webhook subsystem, reconciliation engine | 16 | signature→400, dup event→1 capture; ⚠️ sandbox unverified | `c883348` | n/a | — |
| 13 | 2026-07-06 | **OAT-01** operational acceptance: 10 whole-business scenarios | 26 | money spine passes; ops layer dead-ends documented | `3bd0ba7` | n/a | — |
| 14 | 2026-07-06 | **OAT-02** notification layer: domain events, routing, booking state, incidents | 34 | found + fixed latent double-capture race (FOR UPDATE) | `d66d0af` | n/a | — |
| 15 | 2026-07-09 | **OAT-03** transactional outbox, worker, retry state machine, metrics | 41 | duplicate-worker SKIP LOCKED overlap=0; queue drains to zero | `b7ce97d` | n/a | — |
| 16 | 2026-07-23 | Repo published to GitHub; salvaged early-draft ADRs preserved | 41 | — | `acba734` | ❌ then ✅ | — |
| 17 | 2026-07-23 | **CI fix**: setuptools package discovery (`Multiple top-level packages`) | 41 | clean-venv CI chain reproduced before push | `ea35259` | ✅ | — |
| 18 | 2026-07-23 | **AUDIT-01**: full system audit + project tracking docs + security probes | 53 | **12/12 IDOR/authz probes pass** | `638d06b` | ✅ | — |
| 19 | 2026-07-23 | **MC-01 / SEC-01+SEC-02**: perimeter rate limiting (token bucket, central policies, proxy trust, per-policy failure semantics, metrics) + fail-fast secret validation | 84 | 31 new security tests; 12/12 probes still pass; production+default secrets refuse to start (verified e2e) | `ad93424` | ✅ | — |
| 20 | 2026-07-23 | **MC-02 / FIN-01**: real-SDK webhook validation, payment-environment model, gated Stripe Test Mode suite. **Two production bugs found and fixed** (trust-vs-processing failure conflation; `dict(event)` crash on genuine Stripe events) | 105 (+1 skipped) | 10 real-signature tests, 9 environment tests; replay/tamper/stale rejected; duplicate delivery → single capture; reconciliation ok | `af50c54` | ✅ | — |
| 21 | 2026-07-23 | **MC-03 / BK-01**: unpaid-booking TTL (`payment_expires_at`), explicit `expired` state, first background scheduler (`BookingExpiryWorker`), row-guarded atomic transition, payment-vs-expiry race safety, expiry metrics | 117 (+1 skipped) | 14 deterministic tests incl. concurrent-sweep, late-payment auto-refund, batch isolation; probes + invariants green; migration up/down verified on Postgres | `0dfca83` | ✅ | — |
| 22 | 2026-07-23 | **ci**: pin `ruff==0.15.22` (unpinned linter picked up ruff 0.16 with ~190 new rules and failed a green build) | 117 | fresh-venv CI chain reproduced green | `e17ce99` | ✅ | — |
| 23 | 2026-07-23 | **UI-01**: framework-agnostic design system + runnable web/mobile showcase (`frontend/design-system/`), tokens, components, `/index.html` showcase, analytics taxonomy, DESIGN_SYSTEM/PRODUCT_UX/ANALYTICS_EVENTS docs. **No backend change** | 127 (+1 skipped) | 10 structural contract tests; browser-verified: 0 console errors, interactions work, no horizontal overflow, AA target-size | `21ef1bc` | ✅ | — |
| 24 | 2026-07-23 | flaky-test fix: concurrent-expiry test made deterministic (SQLite can't model row-lock concurrency) | 127 | stable 5x in fresh CI venv | `ea148c7` | ✅ | — |
| 25 | 2026-07-23 | **TST-01**: OpenAPI realigned — code-generated `docs/api/openapi.json` (35 paths, 39 ops) + pytest drift-guard + exporter; three drifted hand specs retired; CI lints the generated spec | 132 (+1 skipped) | 5 contract tests; Spectral 0 errors; full suite + lint green | `5a177d4` | ✅ | — |
| 26 | 2026-07-24 | **TD-01**: Alembic = single schema source of truth. Removed `create_all` + `_apply_postgres_guards` from startup; `ensure_schema()` (apply in local, verify elsewhere); Dockerfile ships migrations; SQLite-vs-Postgres test split | 134 SQLite / 142 with Postgres | 13 migration tests on real Postgres; live app boots via migrations; money-flow smoke recon=0 | `1a6933b` | ✅ | — |
| 27 | 2026-07-24 | **CI-03**: real Postgres service in CI (postgres:16, health-checked, migration-first) + 4 real concurrency tests on Postgres | **148** (+1 gated) | concurrent double-booking→1 (exclusion constraint, 12 threads), concurrent webhook→1 capture (FOR UPDATE), concurrent expiry→1, payment-races-expiry safe (recon=0); all with real row locks. Migrate 2.2s, suite 41s (+9s) | `0f2f8a1` | ✅ | — |
| 28 | 2026-08-03 | **Technical code audit** (read-only): whole repo reviewed + real checks; 4 High / 9 Medium / 9 Low recorded with evidence | 134 | no code changed | `8e6db90` | ✅ | — |
| 29 | 2026-08-03 | **Audit H1**: enforce a single supported currency at listing creation — the ledger sums balances across currencies with no scoping, so a non-PLN listing corrupted escrow math and voided invariant I5 | 138 | 4 tests: default, explicit, case-normalized, non-default → 422 | `ad8222e` | ✅ | — |
| 30 | 2026-08-03 | **Audit H2**: the raw Stripe webhook event is now committed before dispatch, so a failed handler can no longer erase the audit record; insert race handled; retry re-processes | **158** (Postgres) | defect reproduced then proven closed (0 rows → 1 row, same script); 5 new tests + 1 real-Postgres 8-thread race test; warfare + refund_warfare + live_smoke all PASS, recon ok=true grand_total=0 | `08a9549` | ✅ | — |
| 31 | 2026-08-03 | **Audit H3**: the Stripe intent idempotency key is now derived from the booking instead of embedding a uuid4 — a retry after a failed transaction could otherwise create a second intent for the same booking (double charge) | **165** (Postgres) | 7 tests drive the real adapter with a fake SDK: key shape, repeat-call reuse, per-booking uniqueness, independence from amount/host, unchanged destination-charge shape, protocol parity, end-to-end booking id | `4f0747a` | ✅ | — |
| 32 | 2026-08-03 | **Audit H4**: the Stripe call moved out of the row-locked booking transaction; replay heals a booking whose provider call failed | **171** (Postgres) | falsifiable proof: the new Postgres lock test fails against the pre-fix code ("row lock is still held across provider I/O") and passes after; 5 SQLite tests cover provider-down, healing replay, no-second-intent, expired-not-healed; warfare + refund_warfare + live_smoke PASS, recon ok=true | `3e3c3c7` | ✅ | — |
| 33 | 2026-08-04 | **Audit M9a / CI-04**: mypy added as a blocking CI gate (pinned, `app/` at default strictness) and all 8 reported errors fixed — including a real production hole in `ensure_schema()` | **172** (Postgres) | typecheck found: missing revisions verified as "at head" (regression test added), `Payment \| None` returned where `Payment` was promised, `None.processed_at` on the webhook path, None-arithmetic in delivery latency; warfare + refund_warfare + live_smoke PASS, recon ok=true | `9d0c1d0` | ✅ | — |
| 34 | 2026-08-04 | **Audit M9b**: supply-chain and build gates — `pip-audit`, gitleaks, `docker build` + migrations-in-image check, coverage threshold, Dependabot | **172** (unchanged) | CI never built the image despite TD-01 making its contents load-bearing; branch coverage measured honestly at 84.2% (statement-only flattered it to 88%); pip's own CVEs deliberately kept out of the gate | `6921919` | ✅ | — |
| 35 | 2026-08-04 | **Audit M8 / OBS-01**: readiness/liveness split — `/healthz` stays dependency-free, new `/readyz` checks the database; compose probes it | **164** (SQLite; +8) | the readiness test hung, exposing that nothing bounded the TCP *connect* (only the query) — a blackholed database would have stalled the probe ~130s through the very outage it exists to report; all 6 mutations of the new suite caught | `7c74af2` | ✅ | — |
| 36 | 2026-08-04 | **Audit M8 / OBS-02+03**: HTTP metrics middleware (latency, status class, throughput by route template) and business event counters wired to the money/booking funnel | **174** (+10) | route labels were silently prefix-stripped (`/auth/login`, not `/v1/auth/login`) so two routers would have merged into one series; a queued counter survived a no-op rollback and flushed on a later commit; all 6 mutations caught | `feafd58` | ✅ | — |
| 37 | 2026-08-04 | **Audit OBS-06**: Prometheus + Alertmanager in compose, 11 tested alert rules, `homies_database_up` gauge so readiness is alertable, CI job running `promtool check`/`test rules` | **177** (+3) | 11/11 alerts covered by promtool cases incl. 12 must-stay-silent; a pytest cross-checks rule metric names against the live registry (2 seeded typos caught); promtool later run locally after Docker came up — it caught 4 broken test cases (stripped exp_annotations), fixed in `546e2c3`. **Live end-to-end**: Prometheus scrapes the API, 11 rules loaded and all inactive at idle, `homies_database_up=1` collected | `07059a5`+`546e2c3` | ✅ | — |
| 38 | 2026-08-04 | **Verification debt paid**: warfare harnesses re-run against the live stack for the first time in three cycles (Docker became available) | 177 | warfare 7/7, refund_warfare PASS, live_smoke PASS, recon ok=true grand_total=0 in all three. Business counters independently corroborate the harnesses (refunded=5 ↔ 5 refund cycles, payouts=2, succeeded=7); the 200-event webhook replay storm did **not** inflate `payments{succeeded}`, proving the commit-gated counter under real load. 16 distinct route labels, all templates, zero raw UUIDs; zero 5xx | — | — | — |
| 39 | 2026-08-05 | **Audit OBS-07**: ledger-backed KPI endpoint `GET /v1/admin/kpi` — GMV, refunds, recognised commission, payouts, take/refund rate in bps, bookings by status, nights, ADR | **192** (+15 SQLite, +2 Postgres-gated) | 8/8 mutations caught (inclusive window, dropped currency filter, commission derived from GMV, refunds ignored, payout counted as GMV, truncating bps, float ratio, float money); KPI totals cross-checked against `reconcile()` as an independent reader; two mutations initially survived and exposed weak tests, not weak code | _this cycle_ | — | — |

## Notes

- Entries 01–15 predate GitHub publication, so CI status is `n/a` (the workflow
  existed but had never run).
- Build 16 is the first CI execution — it failed, exposing a packaging defect
  that had been latent since build 10 (Alembic added a second top-level package).
  Root cause and fix in build 17.
- No entry has a deployment: the platform has never been deployed to any
  environment.
